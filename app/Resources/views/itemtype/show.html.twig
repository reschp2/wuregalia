{% extends 'base.html.twig' %}

{% block body %}
    <h1>ItemType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ itemType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ itemType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('itemtype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('itemtype_edit', { 'id': itemType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
